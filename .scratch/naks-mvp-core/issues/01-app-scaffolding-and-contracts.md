# 01 — Каркас приложений и базовые контракты

**What to build:** Разработчик может склонировать репозиторий, поставить
зависимости backend и frontend, запустить каждый одной документированной
командой, получить ответ от health-эндпоинта каждого сервиса и увидеть уже
на месте базовые абстракции (интерфейс `LLMProvider`, реестр шаблонов,
реестр АЦ/направлений, Pydantic-модели запросов/ответов будущих эндпоинтов)
как типизированный, покрытый тестовой инфраструктурой каркас — без реальной
бизнес-логики опросника.

**Blocked by:** None — can start immediately.

**Status:** done

- [x] FastAPI-проект в `backend/`: `requirements.txt` + `requirements-dev.txt`,
      `.venv`, версии зафиксированы, установка описана в README, без
      глобальных пакетов.
- [x] React + TypeScript проект в `frontend/` со своим менеджером
      зависимостей (npm) и документированной командой запуска.
- [x] Оба приложения поднимаются одной документированной командой каждое;
      backend отдаёт `GET /health`, frontend отдаёт загружаемую (пусть и
      пустую) корневую страницу.
- [x] Pydantic-модели запроса/ответа для будущих `/api/v1/survey/validate` и
      `/api/v1/documents/generate` покрывают полный набор общих и
      специфичных для «оборудования» полей из User Stories спецификации.
- [x] Определён абстрактный интерфейс `LLMProvider` по контракту из
      спецификации: на вход — только поля, требующие
      нормализации/классификации (никогда ИНН/адрес/ФИО/телефон/email); на
      выход — нормализованное значение по полю + отметка
      «нормализовано»/«нужна проверка» + список замеченных неоднозначностей.
      Конкретная реализация не обязательна в этом тикете.
- [x] Определены структуры `template registry` (код АЦ, направление →
      шаблон) и реестра АЦ, механизм загрузки конфигурации, с единственной
      MVP-записью (`demo`, `equipment`) — сам файл демо-шаблона `.docx`
      может появиться позже, в тикете 02.
- [x] Настроена тестовая инфраструктура backend (`pytest`), минимум один
      проходящий тест (например, на health-эндпоинт).
- [x] Настроена тестовая инфраструктура frontend (`Vitest` + `React Testing
      Library`), минимум один проходящий тест.
- [x] README описывает: установку зависимостей, запуск backend, запуск
      frontend, запуск обоих тестовых наборов.
- [x] Бизнес-логика (реальные поля опросника, правила валидации, генерация
      `.docx`) в этот тикет не входит — это тикет 02.

## Comments

Ревью `mp-code-review` (fixed point `b0f4197`) по обеим осям — без блокеров.

- **Spec:** оба ревьюера подтвердили полное и точное соответствие ticket 01
  (ни одного пропущенного пункта, ни одного scope creep в сторону тикетов
  02-08); независимо перепроверили сомнительный пин `httpx2==2.12.0` в
  `requirements-dev.txt` — реальный пакет, необходим именно для
  `starlette.testclient` при `fastapi==0.141.1`, не опечатка.
- **Standards:** без жёстких нарушений; 4 замечания-suggestion уровня
  (Fowler-смёллы). Исправлено одно (`ValidationWarning` теперь наследует
  `ValidationIssue` вместо дублирования `field`/`code`/`message`, отдельным
  коммитом). Остальные три сознательно не тронуты как преждевременные для
  этого тикета: дублирование формы `AttestationCenterRegistry`/
  `TemplateRegistry` — дождаться третьего реестра в тикете 03 (rule of
  three); top-level `attestation_direction`/`attestation_center_code` в
  `DocumentGenerateRequest` рядом с `normalized_data` — буквально соответствует
  формулировке спеки («принимает нормализованные данные, код направления и
  код АЦ»), сверка/доверие между ними — это часть «недоверия к клиенту» из
  тикета 02, не тема тикета 01; `AttestationCenterRegistry.list_all()` — не
  speculative, понадобится для рендера списка АЦ (User Story 9).
