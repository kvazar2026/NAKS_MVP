# 05 — Встраивание через iframe

**What to build:** Страница виджета из тикета 02 встраивается через
`<iframe>` на отдельной тестовой HTML-странице из репозитория, автоматически
подстраивает высоту через `postMessage` на настроенный целевой `origin`,
принимает тему и идентификатор партнёра через безопасные параметры из
заранее определённого перечня (неизвестный partner-id — откат на демо-
конфигурацию с уведомлением, не ошибка), и отклоняет `postMessage` от
непредусмотренных `origin`.

**Blocked by:** 02.

**Status:** done

- [x] Страница виджета читает `theme` и `partner` из query-параметров;
      `theme` принимает только значения из заранее определённого перечня
      (никакие произвольные CSS/URL/HTML через параметры никогда не
      рендерятся); неизвестный `partner` тихо откатывается на конфигурацию
      `demo` с уведомлением на странице.
- [x] Виджет отправляет `window.parent` сообщения об изменении размера на
      настроенный разрешённый целевой `origin` (не `*` в документированном
      сценарии реального встраивания); входящие `postMessage`, если виджет их
      слушает, проверяют `event.origin` по allowlist перед обработкой.
- [x] В репозитории есть тестовая HTML-страница, встраивающая виджет через
      `<iframe>`, и готовый HTML-пример встраивания в README/документации.
- [x] Бизнес-логика формы не завязана напрямую на факт нахождения внутри
      iframe — контейнер встраивания вынесен в отдельный модуль (проверяется
      по структуре кода, не только по поведению).
- [x] Тесты: значение `theme` вне перечня игнорируется/откатывается на
      значение по умолчанию; неизвестный `partner` откатывается на демо с
      видимым уведомлением; сообщение о ресайзе отправляется при изменении
      содержимого.

**Comments:**

Реализовано. Модуль встраивания — `frontend/src/embedding/`
(`embedConfig.ts` — параметры, `parentMessaging.ts` — контракт postMessage,
`EmbedContainer.tsx` — оболочка). Тестовая страница —
`frontend/public/embed-demo.html`, попадает в сборку и доступна по
`/embed-demo.html`. Пример встраивания продублирован в README.

Решения, которые стоит знать:

- **Целевой origin определяется по referrer, а не берётся из параметра.**
  Параметр, который задаёт встраивающая сторона, не даёт защиты — кто угодно,
  кто вставил iframe, поставит туда что захочет. Виджет берёт origin
  хост-страницы из referrer и отправляет сообщения, только если он есть в
  списке разрешённых. Неизвестный хост не получает ничего: `*` не
  используется никогда. Это fail-closed — при неверной настройке ломается
  авторесайз, а не утекает размер содержимого на чужую страницу.
- **Список разрешённых origin — переменная сборки**
  (`WIDGET_ALLOWED_PARENT_ORIGINS` → build arg `VITE_ALLOWED_PARENT_ORIGINS`),
  потому что Vite вшивает `VITE_*` в бандл. Значит после её изменения нужен
  `docker compose build frontend`, а не рестарт — это отдельно написано в
  `.env.example` и README, иначе на этом легко потерять время. По умолчанию
  пусто = только собственный origin, чего хватает демо-странице.
- **Входящее сообщение одно** — `naks-widget:request-size` (хост просит
  прислать высоту заново после того, как перестроил свою вёрстку). Проверка
  `event.origin` идёт до разбора содержимого сообщения.
- **`SurveyWidget` про iframe ничего не знает** — оболочка добавлена уровнем
  выше, в `App.tsx`. Второй способ встраивания (JS-сниппет), если появится,
  не потребует правок логики формы.
- **`useMemo` в `App.tsx` не косметика:** без него массив разрешённых origin
  получал бы новую идентичность на каждый рендер, и `useEffect` пересоздавал
  бы `ResizeObserver` и слушатель сообщений при каждом изменении состояния
  формы.

Проверено в реальном Chromium, не только в jsdom: на `/embed-demo.html`
высота iframe выставляется в 1958 px, при росте содержимого на 600 px
пересчитывается в 2558 px; referrer доходит до виджета, поэтому целевой
origin резолвится; `?theme=neon` откатывается на `light`;
`?partner=unknown-partner` показывает уведомление. Ни один запрос страницы не
возвращает 4xx.

Не входило в тикет и не делалось: Playwright-тест как часть набора — это
тикет 08 (он же требует Docker Compose, заблокированный тикетом 07).
Браузерная проверка выше была разовой и в репозиторий не коммитится.

Проверки: frontend `npm run test` — 43 passed (было 16), `npm run lint` без
замечаний, `npm run build` (с `tsc -b`) проходит; backend `pytest` — 85
passed (не затронут). `docker compose config` валиден с новым build-arg.

---

**Правка по итогам код-ревью.**

Ревью нашло дыру, которую браузерная проверка первой реализации не поймала:
при недоступном referrer (`referrerpolicy="no-referrer"` на хосте, либо
HTTPS-страница, встроенная по HTTP) виджет вообще не регистрировал слушатель
сообщений, поэтому разрешённый хост не получал ни авторесайза, ни
возможности его запросить — iframe оставался на исходной высоте и обрезал форму.
Демо-страница этого показать не могла: она того же origin, referrer там есть.

Первая попытка починки — «слушать всегда, и пусть хост представится сам
через `naks-widget:request-size`» — в браузере не сработала: хост шлёт
запрос по событию `load` iframe, а это раньше, чем монтируется React,
и сообщение никуда не буферизуется, просто теряется.

Рабочее решение: виджет **сам** объявляет высоту при монтировании, а когда
origin хоста неизвестен — адресует сообщение каждому origin из списка
разрешённых. Это не ослабляет гарантию: `postMessage` доставляется только
тому, чей реальный origin совпал с указанным, поэтому браузер сам выбирает
нужного адресата и отбрасывает остальных; `*` по-прежнему не используется
нигде. Проверено в Chromium на двух разных origin: разрешённый хост получает
высоту (1958 px) при полностью отсутствующем referrer, неразрешённый —
пустой список сообщений.

Побочный плюс: хосту теперь не нужно ничего запрашивать, пример встраивания
стал короче. `naks-widget:request-size` остался для пересинхронизации.

Прочее по тому же ревью: `?partner=constructor` (и `toString`, `valueOf`,
`__proto__`) считался известным партнёром — обычный `PARTNERS[key]` достаёт
свойства с `Object.prototype`; заменено на `Object.hasOwn`. Тема переехала на
корень документа: фон, покрашенный только на обёртке виджета, оставлял белый
холст под содержимым в тёмной теме, а растянуть обёртку нельзя — её высота и
есть то, что уходит хосту для авторесайза.
