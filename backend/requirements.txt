# Runtime dependencies. Versions are pinned exactly for reproducible installs.
# Install with: pip install -r requirements.txt
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic-settings==2.15.0
PyYAML==6.0.3
# .docx generation for POST /api/v1/documents/generate (ticket 02). docxtpl
# fills Jinja-style {{ placeholder }} syntax embedded in the .docx template
# itself (spec: "через шаблонизацию плейсхолдеров... не поиском-заменой
# текста напрямую"), wrapping python-docx.
docxtpl==0.20.2
python-docx==1.2.0
lxml==6.1.2
Jinja2==3.1.6
MarkupSafe==3.0.3
# Anthropic SDK for AnthropicProvider (ticket 04). Runtime, not dev-only: it
# is imported by app/services/provider_registry.py at startup regardless of
# which provider is selected. Only reached over the network when
# NAKS_LLM_PROVIDER=anthropic.
anthropic==1.2.0
